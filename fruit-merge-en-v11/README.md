# Original root-level pages from fruit-merge-en-v11

These are exact copies of the two existing **root-level preview pages** in the v11 project, not copies or path-adjusted versions of the `site/` publication pages. They retain the original small article-only structure generated by `build.py` and use `../style.css` and `../index.html` for local preview.

Copy the two folders into the root of `fruit-merge-en-v11/` if you need to restore those original preview files. This archive contains no `site/` directory and does not change the published pages.
